[pip.deps.kernel-tuner]
version = ">=1.5.0"   # for production
# version = "@ ../kernel_tuner" # for local
# version = "@ git+https://github.com/KernelTuner/kernel_tuner.git@julia" # for CI
# editable = true
